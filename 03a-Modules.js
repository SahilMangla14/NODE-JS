// Modules
const john = 'john';
const peter = 'peter';

const sayHi = (name) =>{
    console.log(`Hello there ${name}`);
}

sayHi('susan');
sayHi(john);
sayHi(peter);


// It will be better if we have names in different file and rest code in differnt file
// All js files are nothing but modules